# Normalise les fins de ligne : LF partout dans le repo.
* text=auto eol=lf

# Fichiers qui DOIVENT rester en LF (sinon cassent en conteneur Linux).
*.sh text eol=lf
docker-entrypoint.sh text eol=lf

# Binaires
*.png binary
*.jpg binary
*.svg text
*.woff2 binary
